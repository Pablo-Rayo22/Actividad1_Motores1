using UnityEngine;
using UnityEngine.InputSystem;

public class DetectorTriggerPuerta : MonoBehaviour
{
    // Start se llama una vez antes de la ejecución del Update después de que se crea el script
    void Start()
    {
        
    }

    // Update se llama una vez por frame (cada pintada de pantalla)
    void Update()
    {
        
    }
}
